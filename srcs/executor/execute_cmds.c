#include <executor.h>

// This is basically pipex

void	execute_cmd(void)
{
	// open pipes?
	// execve
}

void	execute_all_cmds(void)
{
	// loop over commands
	// execute each cmd
	// check exit code of last command
}