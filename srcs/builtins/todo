builtin functions will be in this folder:
echo
	- with option -n
cd
	- only relative or absolute path
pwd
export
unset
env
exit

// signal handlers here?
// header file maken voor builtins? - check exit.c


export

export HOME=$HOME:daan		werkt
export HOME=$HOMEdaan		werkt niet en wordt een newline